{% extends 'base.html.twig' %}

{% block title %}ajout{% endblock %}

{% block body %}
    <div id="listes">
        <h2>Personnels :</h2>
        <div id="utilisateursListes">
            {% for u in listeUtilisateurs %}
                <ul>
                    <li>
                        <p>{{ u.nom }} {{ u.prenom }}, {{ u.email }}</p><a href="{{ path('accueil_supprimer', {'utilisateur': utilisateurID.id}) }}">Supprimer</a>
                    </li>
                </ul>
            {% endfor %}
        </div>
        <h2>Formations :</h2>
        <div>

        </div>
    </div>
{% endblock %}
